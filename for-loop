#include <stdio.h>

int main() {
    int i,n,result=0;
    printf("Enter the value of n:");
    scanf("%d",&n);
    for(i=1;i<=n;i++) {
        result += i*i;
    }
    printf("%d",result);

    return 0;
}
